var gotSwag = require ( '../' );

describe( 'The monkeyParameter function', function () {

  it.skip( 'should generate path parameters', function ( done ) {

  } );

  it.skip( 'should generate query parameters', function ( done ) {

  } );

  it.skip( 'should generate body parameters', function ( done ) {

  } );

  it.skip( 'should generate header parameters', function ( done ) {

  } );

} );
